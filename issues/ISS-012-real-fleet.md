# ISS-012: Real-fleet adapter (second provider)

- Status: future
- Source: Track C future-item 1; LIMITATIONS §1 "no real fleet"
- Problem: everything is synthetic until a real provider export flows end-to-end.
- Proposal: copy the Vapi pattern → second-provider adapter + synthetic sample (minimal);
  then live-pull CLI + real export fixture + dashboard provenance (full).
- Accept when: a real-shape export flows price→verdict→detect with ABSENT honesty intact.
