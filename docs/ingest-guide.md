# Ingest guide

The single source of truth is [INGEST.md](INGEST.md): the call object
shape, the provider adapters, and the honest omissions (what "no data"
means versus zero).

Run it:

```bash
uv run python -m turnstile_ingest --sample
```
